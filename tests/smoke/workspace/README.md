# CGENV Remote SSH smoke workspace

This file exists only inside the disposable SSH target. A successful smoke test
opens `/workspace` through the repository's `scripts/cgenv` launcher and renders
this remote folder in the graphical VS Code client.
